# Bug 修复内容

---

## v2.0 修复（2026-06-07）

### Bug #1: 输入框 Backspace/Delete 键失效

- **发现版本**: v1.0 (0521)
- **根因**: 全局 keydown 事件监听器 `preventDefault()` 拦截了退格和删除键
- **影响范围**: 所有文本输入框
- **修复**: 移除全局键盘拦截，仅在 IV 手动键入框上做字符白名单校验（允许 Backspace/Delete/方向键/Tab/Esc）
- **修复版本**: v2.0 (0607)

### Bug #2: CSV 导入后数据展示混乱

- **发现版本**: v1.0 (0521)
- **根因**: `split(',')` 无法处理 CSV 双引号包裹的字段。`Number('"0"')` 返回 `NaN`，空字段 `""` 被当作字符串 `'""'` 而非空值
- **影响范围**: CSV 导入功能全部数据
- **修复**: 替换为逐字符状态机解析器（`parseLine`），正确处理：
  - 双引号包裹字段（`"白金独角兽"` → `白金独角兽`）
  - 空引号字段（`""` → `""`，转为空数组或 0）
  - 引号内分号分隔符（`"巨灵组;动物组"` → `巨灵组;动物组`）
  - 引号内逗号（双引号转义 `"a""b"` → `a"b`）
- **修复版本**: v2.0 (0607)

### Bug #3: CSV 导入中文乱码

- **发现版本**: v2.1 (0608)
- **根因**: 用户 CSV 文件为 GBK 编码（无 BOM），代码中 `FileReader.readAsText(file, 'UTF-8')` 硬编码 UTF-8 解码
- **影响范围**: 导入 GBK 编码的 CSV 文件
- **修复**: 改用 `readAsArrayBuffer` 读取原始字节 → 先 UTF-8 解码 → 检测是否含"名称"等中文字段名 → 不含则自动回退 GBK 解码
- **修复版本**: v2.1 (0608)

### Bug #4: 种公统计面板展示格式混乱

- **发现版本**: v1.0 (0521)
- **根因**: 表格使用固定像素宽度，无横向滚动条，无响应式设计
- **影响范围**: 种公统计弹窗在小屏幕或大量数据时
- **修复**: 弹性表格布局 + `overflow: auto` 横向滚动 + `position: sticky` 固定表头 + 响应式断点
- **修复版本**: v2.0 (0607)

### Bug #5: 蛋组数据不完整

- **发现版本**: v1.0 (0521)
- **根因**: 旧内联 JSON 仅手工录入约 200 种精灵，且缺失大量带括号后缀的形态变体
- **影响范围**: 名称自动补全覆盖不全，蛋组查询缺失
- **修复**: v2.0 从 Wiki 源 JSON（430 条目）提取完整映射 → v2.1 进一步从精灵描述.json（1065 条目）提取 444 条去重映射
- **修复版本**: v2.0 (0607) / v2.1 (0608)

### Bug #6: 特长、奖牌、名称自动补全不显示

- **发现版本**: v2.1 (0608)
- **根因**: `SPECIALTIES.map(...).replace(...)` — 对数组调用 `.replace()`，JavaScript 抛出 `TypeError`，阻断 `FormManager.init()` 后续执行。特长渲染失败 → 奖牌渲染未执行 → `Autocomplete.setup()` 未执行
- **影响范围**: 添加表单和编辑弹窗的特长/奖牌控件、名称自动补全全部不可用
- **修复**: 在 `.map()` 后添加 `.join('')` 先转为字符串，再调用 `.replace()`
- **修复版本**: v2.1 (0608)

### Bug #7: 添加表单字段未拆分

- **发现版本**: v2.1 (0608)
- **根因**: `f_type` 旧单字段残留于 HTML，批量替换脚本未匹配到浏览器保存的编码格式
- **影响范围**: 添加表单仍显示旧的"血脉/属性"合一场
- **修复**: 直接编辑替换为 `f_attribute`（灰底只读）+ `f_bloodline`（可编辑）
- **修复版本**: v2.1 (0608)

---

## v2.2 修复（2026-06-09）

### Bug #8: 表格表头与数据列错位

- **发现版本**: v2.1 (0609)
- **根因**: 数据行渲染中 `<td>'+s.bloodline+'</td>` 占用了属性列位置，缺少 `<td>'+s.attribute+'</td>`，导致后续所有列左移一位
- **影响范围**: 导入 CSV 后表格所有列与表头不对齐
- **修复**: 在数据行渲染中补上 `s.attribute` 列：`<td>'+s.name+'</td><td>'+s.attribute+'</td><td>'+s.bloodline+'</td>...`
- **修复版本**: v2.2 (0609)

### Bug #9: 种公统计性格矩阵无法单击选择

- **发现版本**: v2.1 (0608)
- **根因**: `onclick="StatsModal._toggleNature("逞强")"` 中内外双引号冲突，HTML 属性解析时 `"` 提前闭合 onclick 属性值
- **影响范围**: 种公统计面板的性格矩阵——只能通过行/列标题批量选择，无法单击单个性格格
- **修复**: 改为 `onclick="StatsModal._toggleNature('逞强')"`，与主筛选矩阵统一使用 `\'` 转义单引号包裹参数
- **修复版本**: v2.2 (0609)

### Bug #10: 主筛选矩阵容器遮挡上方按钮

- **发现版本**: v2.1 (0609)
- **根因**: 浏览器保存 HTML 时在 `.matrix-wrapper` 上注入 `transform: translate(33px, -45px)` 内联样式，使矩阵向上偏移 45px 覆盖筛选芯片
- **影响范围**: 展开筛选矩阵时遮挡上方筛选芯片按钮
- **修复**: 移除浏览器保存时注入的内联 `transform` 样式
- **修复版本**: v2.2 (0609)

### Bug #11: 页面加载时种公统计弹窗自动弹出

- **发现版本**: v2.1 (0609)
- **根因**: 浏览器保存页面时种公统计弹窗处于打开状态，`class="modal-overlay active"` 被写入 HTML 文件
- **影响范围**: 每次打开 HTML 种公统计弹窗默认显示
- **修复**: 移除 statsModal 的 `active` 类
- **修复版本**: v2.2 (0609)

### Bug #12: 种公统计蛋组筛选逻辑错误

- **发现版本**: v2.1 (0608)
- **根因**: 蛋组筛选使用了精灵数据过滤（`.every()` AND 模式）而非列可见性控制。用户期望勾选蛋组时隐藏其他列，而非过滤精灵
- **影响范围**: 种公统计蛋组筛选行为不符合预期
- **修复**:
  - 蛋组筛选改为列可见性控制：`el = this._eggFilter.slice().sort()` 直接用作显示的列
  - AND 模式改为 OR 模式（列显示/隐藏不需要逻辑连接符）
  - 删除蛋组对精灵数据的过滤行
- **修复版本**: v2.2 (0609)

### Bug #13: 种公统计蛋组标签布局错位

- **发现版本**: v2.1 (0609)
- **根因**: 浏览器保存时在蛋组标签容器和性格矩阵上注入了多处 `transform: translate()` 内联样式，在缩放或不同分辨率下导致元素重叠/错位
- **影响范围**: 缩小浏览器缩放比例时蛋组选择标签跑出可视区域或重叠
- **修复**: 将性格矩阵和蛋组标签合并到同一个 flex 容器行内（`display:flex;align-items:flex-start;gap:16px`），矩阵在左、标签在右，清除所有浏览器注入的内联样式
- **修复版本**: v2.2 (0609)
