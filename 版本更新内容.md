# 版本更新内容

---

## v2.1 — 精灵数据源重构与属性系统拆分（2026-06-08）

### 一、精灵数据源重构

#### 1.1 数据文件

| 文件              | 用途                    | 说明                                     |
| ----------------- | ----------------------- | ---------------------------------------- |
| `精灵描述.json` | 精灵综合数据（1065 条） | 含基础信息、属性、蛋组、种族值、进化链等 |
| `蛋组描述.json` | 蛋组编号映射            | 将蛋组数字 ID（1-15）映射为中文名称      |

#### 1.2 数据处理流程

```
精灵描述.json (1065 entries)
  → 按 species_id 去重
  → 提取 form 字段（default 形态取 localized.zh.name）
  → 提取 breeding_profile.egg_groups (数字 ID 数组)
  → 蛋组描述.json 映射 数字 ID → 中文蛋组名
  → 构建 SPRITE_DATA: { 精灵名 → { eggGroups, attribute, bloodline } }
  → 构建 NAME_LIST: 精灵名称列表（用于输入自动补全）
  → 内联注入 HTML <script> 块
```

#### 1.3 与旧版对比

|          | v2.0（s2 JSON）          | v2.1（精灵描述.json）                     |
| -------- | ------------------------ | ----------------------------------------- |
| 数据来源 | rocom_egg_groups_s2.json | 精灵描述.json + 蛋组描述.json             |
| 精灵条目 | 430 → 去重 347          | 1065 → 去重**444**                 |
| 蛋组存储 | 直接存中文名             | 数字 ID → 映射为中文名                   |
| 属性信息 | 无                       | 含主属性 + 副属性                         |
| 名称来源 | JSON name 字段           | form 字段（default 取 localized.zh.name） |

---

### 二、属性 / 血脉字段拆分

#### 2.1 数据模型变更

**旧**（v2.0）：

```yaml
精灵实体模型:
  type: string  # 血脉/属性合一
```

**新**（v2.1）：

```yaml
精灵实体模型:
  attribute: string   # 属性（只读，自动匹配）
  bloodline: string   # 血脉（可修改）
```

#### 2.2 字段语义

| 字段                  | 来源                                         | 可否修改  | 示例                   |
| --------------------- | -------------------------------------------- | --------- | ---------------------- |
| `attribute`（属性） | `main_type` + `/` + `sub_type`（如有） | ❌ 只读   | "光"、"火/萌"、"翼/恶" |
| `bloodline`（血脉） | 留空，手动输入                               | ✅ 可修改 | ""、"奇异"、"污染"     |

#### 2.3 双属性精灵

精灵描述中有 202 个精灵拥有双属性（同时具有 main_type 和 sub_type），属性字段自动合并为主/副格式：

| 精灵     | 属性  | 说明   |
| -------- | ----- | ------ |
| 迪莫     | 光    | 单属性 |
| 喵喵     | 草    | 单属性 |
| 治愈兔   | 火/萌 | 双属性 |
| 恶魔叮   | 翼/恶 | 双属性 |
| 蹦蹦种子 | 草/毒 | 双属性 |

#### 2.4 UI 变更

| 位置         | 变更                                                       |
| ------------ | ---------------------------------------------------------- |
| 添加表单     | 属性字段（灰底只读）+ 血脉字段（可编辑，已移除旧 f_type）  |
| 编辑弹窗     | 同上                                                       |
| 数据表格     | 新增"属性"列（排在名称之后），"血脉"列保留                 |
| 筛选面板     | 新增 🔮 属性筛选芯片 + 🏷 血脉筛选芯片（替代旧的类型筛选） |
| 名称自动补全 | 下拉列表增加属性预览（如"喵喵 ·草"、"治愈兔 ·火/萌"）    |

#### 2.5 CSV 格式变更

**新格式**（v2.1）：

```
名称,属性,血脉,性格,性别,特长,奖牌,蛋组,生命,物攻,魔攻,物防,魔防,速度,盒子
```

#### 2.6 数据迁移

旧 LocalStorage 数据（`rocom_sprites_v4`）在加载时自动迁移：

- `sprite.type`（旧字段）→ `sprite.bloodline`（新字段）
- `sprite.attribute` 初始化为空字符串
- `sprite.box` 缺失时补为空字符串

---

### 三、CSV 导入编码自适应

#### 3.1 问题

`rocom_sprites_20260520.csv` 为 **GBK 编码**（无 BOM），之前的 `FileReader.readAsText(file, 'UTF-8')` 导致中文乱码。

#### 3.2 修复

```js
// 1. 使用 readAsArrayBuffer 读取原始字节
// 2. 先尝试 UTF-8 解码
// 3. 若解码后不含"名称"字段 → 自动回退 GBK 解码
// 4. 再失败 → 尝试 GB2312 解码
```

编码检测完全自动化，用户无需手动选择。

---

### 四、种公统计面板增强

#### 4.1 性格筛选改为矩阵

| 原来                  | 现在                                     |
| --------------------- | ---------------------------------------- |
| `<select>` 下拉单选 | **6×6 性格矩阵**（多选，OR 逻辑） |
| —                    | 点击行/列标题批量选择/取消               |
| —                    | 选中态蓝色高亮                           |

#### 4.2 蛋组筛选改为标签勾选

| 原来                   | 现在                                                   |
| ---------------------- | ------------------------------------------------------ |
| `<select>` 下拉单选  | **标签 checkbox 组**（多选，AND 逻辑）           |
| 仅显示数据中存在的蛋组 | 始终显示**全部 14 个可孵蛋组**（排除"无法孵蛋"） |
| —                     | "↺ 清空筛选"按钮                                      |

#### 4.3 布局

```
[视角切换] [详细个体值开关]
[🧬 性格矩阵] [🥚 蛋组标签组] [清空筛选]
[摘要信息]
```

---

### 五、表单布局优化

#### 5.1 性格矩阵 + 个体值并排

添加表单和编辑弹窗中，性格矩阵（左）和 IV 滑块组（右）使用 flex 布局并排显示，自适应换行。

#### 5.2 个体值输入

保留 5 档位滑块（0/7/8/9/10），新增点击数值手动键入：

- 点击数值标签 → 切换为输入框，自动 focus
- 键入数字后 Enter/失焦 → 校验
  - 有效值（0/7/8/9/10）→ 接受，同步滑块
  - 无效值 → ❌ 红色 toast 提示"输入值无效，仅允许 0、7、8、9、10"，恢复原值
- Esc → 取消编辑

#### 5.3 精灵名称自动联想

- 数据源：精灵描述.json，444 个名称
- 输入时实时过滤，键盘 ↑↓ 选择，Enter 确认
- 下拉列表显示蛋组 + 属性预览
- 提交时校验名称必须在列表内

---

### 六、其他新增功能

#### 6.1 特长单选限制

- 从 checkbox 多选改为 **radio button 互斥组**（芯片样式）
- 每只精灵最多拥有一个特长
- 默认选中"无"

#### 6.2 盒子标签

- 精灵新增 `box` 字段（所在盒子名称）
- 表单：文本输入 + 已有盒子 datalist 自动补全
- 表格：📦 青色标签
- 筛选：📦 芯片 + 浮层多选
- CSV：最后一列

#### 6.3 奖牌扩展

- 新增"炫彩"和"异色"两个奖牌选项（从 6 个增至 8 个）

#### 6.4 配种推荐

- 选中精灵 → 💕 按钮 → 弹窗
- 匹配条件：性别相反 + 蛋组有交集
- 排序优先级：IV 非零位置重叠数（降序）→ 性格相同
- 分级：最优（绿）/ 次优（紫）/ 一般（黄）/ 基础（灰）

#### 6.5 批量删除

- 表头 checkbox 全选/取消 + 行 checkbox 单选
- 批选栏：全选筛选结果 / 选中筛选结果 / 取消 / 批量删除

#### 6.6 种公统计显示模式

- 详细个体值开关（ON：显示非零 IV emoji+数值；OFF：仅显示名称）
- 性格 × 蛋组 / 蛋组 × 性格 两种视角切换

---

---

## v2.2 — 盒子整理 + 种公统计增强（2026-06-09）

### 一、精灵盒子整理侧边栏

#### 1.1 入口

筛选面板标题右侧 **📦 整理精灵** 按钮。

#### 1.2 布局

点击后右侧展开 **380px 侧边栏**，与表格 flex 并排显示（非遮罩弹窗），勾选精灵和选择盒子可同时进行。

```
┌──────────────────────────┬─────────────┐
│  表格（checkbox 可操作） │ 📦 盒子侧栏  │
│  ☑ 精灵A                 │ ┌──┐ ┌──┐  │
│  ☑ 精灵B                 │ │盒1│ │盒2│  │
│  ☐ 精灵C                 │ └──┘ └──┘  │
│  ...                     │ ┌──┐ ┌──┐  │
│                          │ │盒3│ │ + │  │
│                          │ └──┘ └──┘  │
│                          │ [移动] [撤销] │
└──────────────────────────┴─────────────┘
```

#### 1.3 功能

| 功能      | 说明                                               |
| --------- | -------------------------------------------------- |
| 盒子来源  | 自动收集现有精灵的 box 字段 + 手动创建             |
| 卡片展示  | 2 列网格，每个卡片显示名称 + "X / 30"              |
| 容量上限  | 30 只/盒，满额阻止移动并提示                       |
| 移动操作  | 勾选精灵 → 点击目标盒子（蓝色高亮）→ 点"📥 移动" |
| 撤销/重做 | 单步撤销 → 按钮变为"↪ 重做"→ 可恢复             |
| 新建盒子  | 点击 "+" 卡片 → 输入名称 → 回车确认              |
| 删除盒子  | hover 卡片右上 × → confirm → 精灵移至"未分类"   |
| 未分类    | 始终显示为首位卡片，不可删除（box="" 的精灵）      |

#### 1.4 状态管理

- `BoxPanel._undoStack`: 撤销栈，存储 `{entries: [{id, fromBox}], toBox}`
- `BoxPanel._redoStack`: 重做栈
- `BoxPanel._customBoxes`: 用户手动创建的盒子名（即使为空也保留）

### 二、种公统计增强

| 改动             | 说明                                                                 |
| ---------------- | -------------------------------------------------------------------- |
| 蛋组筛选逻辑     | 从精灵数据过滤改为**列可见性控制**——勾选蛋组 A 则只显示 A 列 |
| 蛋组筛选模式     | AND →**OR**（列显示只需匹配任一勾选项）                       |
| 性格矩阵交互修复 | 单击单个性格格可切换选中（引号冲突修复）                             |
| 布局合并         | 性格矩阵和蛋组标签合并为一行 flex 布局，缩放不再错位                 |
| 导出 CSV         | 新增"📥 导出 CSV"按钮，导出当前视角网格为 UTF-8 BOM CSV              |
| 自动弹出修复     | 移除浏览器保存时注入的 `active` 类，不再默认弹出                   |

---

### 四、文件清单

| 文件                         | 说明                                 |
| ---------------------------- | ------------------------------------ |
| `精灵盒子管理器0609.html`  | 最新版管理器（v2.2），单文件，零依赖 |
| `精灵描述.json`            | 精灵综合数据源（1065 条），定期更新  |
| `蛋组描述.json`            | 蛋组 ID → 中文名映射（15 组）       |
| `rocom_egg_groups_s2.json` | 蛋组数据（备用，已格式化）           |
| `项目开发文档.md`          | 项目架构与开发规范文档               |
| `版本更新内容.md`          | 本文件                               |
| `bug修复内容.md`           | 全部 Bug 修复记录（13 条）           |
